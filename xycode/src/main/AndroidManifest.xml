<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.xmethod.xycode">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name="xyz.xmethod.xycode.utils.crashUtil.CrashActivity"
            android:screenOrientation="nosensor" />
        <activity
            android:name="xyz.xmethod.xycode.utils.debugger.DebugActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/ServerControllerTheme"
            android:screenOrientation="nosensor" />
        <activity
            android:name="xyz.xmethod.xycode.utils.downloadHelper.CompulsiveHelperActivity"
            android:theme="@style/UpdateDialogTheme"
            android:screenOrientation="nosensor" />
        <activity
            android:name="xyz.xmethod.xycode.utils.serverApiHelper.ServerControllerActivity"
            android:theme="@style/ServerControllerTheme"
            android:screenOrientation="nosensor" />
    </application>

</manifest>
